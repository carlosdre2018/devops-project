version: 1.0
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
steps:
  - step: 
      name: "Build Docker Image"
      shell: bash
      commands:
        - echo "Construyendo imagen Docker..."
